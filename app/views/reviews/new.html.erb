<div class="container">
  <div class="row">
    <div class="col-6 mx-auto">
      <h1>Your review:</h1>
        <%= simple_form_for([@cocktail, @review]) do |f| %>
          <%= f.input :content %>
          <%= f.input :rating %>
          <%= f.button :submit, class: "btn-success" %>
        <% end %>
    </div>
  </div>
</div>
